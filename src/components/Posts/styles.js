import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
    mainContainer : {
        display : 'flex',
        alignContent : 'center'
    },
    smMargin : {
        margin: theme.spacing(1)
    },
    actionDiv : {
        textAlign : 'center'
    }
}))